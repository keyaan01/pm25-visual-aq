# Core deep-learning stack. On Google Colab, torch/torchvision come pre-installed
# with CUDA, so pip will skip them (the loose >= pins are already satisfied) and you
# keep GPU support. Locally (CPU) these install the CPU build, which is fine for the
# small smoke-tests.
torch>=2.2
torchvision>=0.17
timm>=1.0.0

# Data + images
datasets>=2.19
huggingface_hub>=0.23
pillow>=10.0
opencv-python>=4.9
imagehash>=4.3

# Scientific stack
numpy>=1.26
pandas>=2.0
scikit-learn>=1.4
scipy>=1.11

# Utilities
matplotlib>=3.8
tqdm>=4.66
pyyaml>=6.0
requests>=2.31
